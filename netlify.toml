[build]
  command = "npm ci && npx vite build"
  publish = "dist"
  base = "/"

[context.production]
  environment = { NODE_ENV = "production" }

[context.staging]
  environment = { NODE_ENV = "staging" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
